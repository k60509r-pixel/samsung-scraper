name: Daily Recycle Price Scraper

on:
  schedule:
    # 每天台灣時間 06:00 = UTC 22:00（前一天）
    - cron: "0 22 * * *"
  workflow_dispatch:

jobs:
  scrape-samsung:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: playwright install chromium --with-deps

      - name: Run Samsung scraper
        run: python scraper.py

      - name: Write Samsung to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets.py results.json

      - name: Upload Samsung results artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-samsung-${{ github.run_number }}
          path: results.json
          retention-days: 30

  scrape-ipad:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: playwright install chromium --with-deps

      - name: Run iPad scraper
        run: python scraper_ipad.py

      - name: Write iPad to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_ipad.py results_ipad.json

      - name: Upload iPad results artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-ipad-${{ github.run_number }}
          path: results_ipad.json
          retention-days: 30

  scrape-oppo:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: playwright install chromium --with-deps

      - name: Run OPPO scraper
        run: python scraper_oppo.py

      - name: Write OPPO to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_oppo.py results_oppo.json

      - name: Upload OPPO results artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-oppo-${{ github.run_number }}
          path: results_oppo.json
          retention-days: 30

  scrape-vivo:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: playwright install chromium --with-deps

      - name: Run VIVO scraper
        run: python scraper_vivo.py

      - name: Write VIVO to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_vivo.py results_vivo.json

      - name: Upload VIVO results artifact
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-vivo-${{ github.run_number }}
          path: results_vivo.json
          retention-days: 30

  scrape-google:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11", cache: "pip" }
      - run: pip install -r requirements.txt
      - run: playwright install chromium --with-deps
      - run: python scraper_google.py
      - name: Write Google to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_google.py results_google.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-google-${{ github.run_number }}
          path: results_google.json
          retention-days: 30

  scrape-asus:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11", cache: "pip" }
      - run: pip install -r requirements.txt
      - run: playwright install chromium --with-deps
      - run: python scraper_asus.py
      - name: Write ASUS to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_asus.py results_asus.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-asus-${{ github.run_number }}
          path: results_asus.json
          retention-days: 30

  scrape-realme:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11", cache: "pip" }
      - run: pip install -r requirements.txt
      - run: playwright install chromium --with-deps
      - run: python scraper_realme.py
      - name: Write Realme to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_realme.py results_realme.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-realme-${{ github.run_number }}
          path: results_realme.json
          retention-days: 30

  scrape-xiaomi:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11", cache: "pip" }
      - run: pip install -r requirements.txt
      - run: playwright install chromium --with-deps
      - run: python scraper_xiaomi.py
      - name: Write Xiaomi to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_xiaomi.py results_xiaomi.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-xiaomi-${{ github.run_number }}
          path: results_xiaomi.json
          retention-days: 30

  scrape-senao:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.11", cache: "pip" }
      - run: pip install -r requirements.txt
      - run: playwright install chromium --with-deps
      - run: python scraper_senao.py
      - name: Write Senao to Google Sheets
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SPREADSHEET_ID: ${{ secrets.SPREADSHEET_ID }}
        run: python write_to_sheets_senao.py results_senao.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: scrape-senao-${{ github.run_number }}
          path: results_senao.json
          retention-days: 30
